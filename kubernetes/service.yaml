apiVersion: v1
kind: Service

metadata:
  name: enterprise-cloud-platform-service
  namespace: demo

spec:
  type: LoadBalancer

  selector:
    app: enterprise-cloud-platform

  ports:
  - port: 80
    targetPort: 80